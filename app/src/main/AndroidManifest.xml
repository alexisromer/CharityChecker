<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.charitychecker">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CharityChecker">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${API_MAPS}" />

        <activity
            android:name=".FoodBankMapActivity"
            android:exported="false"
            android:label="@string/title_activity_food_bank_map" />

        <meta-data
            android:name="keyValue"
            android:value="${KEY}" />
        <meta-data
            android:name="idValue"
            android:value="${ID}" />

        <activity
            android:name=".bloodActivity"
            android:exported="false" />
        <activity
            android:name=".charityActivity"
            android:exported="false" />
        <activity
            android:name=".optionsActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>